name: PHASE 09 documents reports audit backup

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]
    paths:
      - '.github/workflows/phase09-ci.yml'
      - 'database/**'
      - 'docs/execution/POSMAN-PHASE-09-EXECUTION-PACK.md'
      - 'docs/architecture/phase-09-documents-reports-audit-backup.md'
      - 'docs/PHASE-09-REPORT.md'
      - 'scripts/verify_phase09.py'
      - 'scripts/phase09_policy.py'
      - 'src-tauri/**'
      - 'src/platform/tauri/phase09/**'
      - 'src/features/phase09/**'
      - 'src/app/AppRoot.tsx'
      - 'src/i18n/**'
      - 'tests/**'
      - 'package.json'
      - 'package-lock.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: PHASE 09 implementation pending final CI replacement
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary bootstrap placeholder; final read-only validation workflow will replace this file.'
